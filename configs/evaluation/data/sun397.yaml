dataset_name: sun397
cache_dir: ./.cache
data_dir: ./data/SUN397

image_size: 96  # Match the model's input size

train_split: train
val_split: val
test_split: test

# No augmentation (only for evaluation)
augmentation: null
