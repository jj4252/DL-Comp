dataset_name: cub200
cache_dir: ./.cache
data_dir: ./data/CUB-200

# Image preprocessing
image_size: 96  # Match the model's input size

# Data splits
train_split: train
val_split: val
test_split: test

# No augmentation (only for evaluation)
augmentation: null
