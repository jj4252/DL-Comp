dataset_name: mini_imagenet
cache_dir: ./.cache
data_dir: ./data/mini-ImageNet

# Image preprocessing
# TODO: double check 'image_size' (for all data configs)
image_size: 96  # Match the model's input size

# Data splits
train_split: train
val_split: val
test_split: test

# No augmentation (only for evaluation)
augmentation: null
